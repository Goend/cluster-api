---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ansiblecontrolplanetemplates.controlplane.cluster.x-k8s.io
spec:
  group: controlplane.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: AnsibleControlPlaneTemplate
    listKind: AnsibleControlPlaneTemplateList
    plural: ansiblecontrolplanetemplates
    singular: ansiblecontrolplanetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnsibleControlPlaneTemplate is the Schema for the control plane
          templates API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnsibleControlPlaneTemplateSpec defines the desired state
              of AnsibleControlPlane in a ClusterClass.
            properties:
              template:
                description: AnsibleControlPlaneTemplateResource describes the data
                  needed to create a control plane from a template.
                properties:
                  spec:
                    description: AnsibleControlPlaneSpec defines the desired state
                      of an Ansible-based control plane.
                    properties:
                      ansibleConfigSpec:
                        description: AnsibleConfigSpec carries the bootstrap configuration
                          driven by the Ansible Bootstrap Provider.
                        properties:
                          certRef:
                            description: CertRef points to the secret containing tls.crt
                              and tls.key which must be baked into the node before
                              ansible runs.
                            properties:
                              name:
                                description: name is unique within a namespace to
                                  reference a secret resource.
                                type: string
                              namespace:
                                description: namespace defines the space within which
                                  the secret name must be unique.
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          clusterOperationTemplate:
                            description: ClusterOperationTemplate defines the parameters
                              required to compose a kubean ClusterOperation resource
                              automatically.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations propagated to the created
                                  object.
                                type: object
                              apiVersion:
                                description: APIVersion of the target resource, e.g.
                                  clusterops.kubean.io/v1alpha1.
                                type: string
                              kind:
                                description: Kind of the target resource, e.g. ClusterOperation.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels propagated to the created object.
                                type: object
                              namespace:
                                description: Namespace of the resource. Defaults to
                                  the namespace of the AnsibleConfig.
                                type: string
                              spec:
                                description: Spec encodes the spec body that should
                                  be copied to the created object.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - apiVersion
                            - kind
                            type: object
                          clusterOpsRef:
                            description: |-
                              ClusterOpsRef contains the template required to create a kubean ClusterOperation resource.
                              Deprecated: prefer ClusterOperationTemplate for auto-generated kubean resources.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations propagated to the created
                                  object.
                                type: object
                              apiVersion:
                                description: APIVersion of the target resource, e.g.
                                  cluster.kubean.io/v1alpha1.
                                type: string
                              kind:
                                description: Kind of the target resource, e.g. Cluster
                                  or ClusterOperation.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels propagated to the created object.
                                type: object
                              name:
                                description: Name of the resource to be created.
                                type: string
                              namespace:
                                description: Namespace of the resource. Defaults to
                                  the namespace of the AnsibleConfig.
                                type: string
                              spec:
                                description: Spec encodes the spec body that should
                                  be copied to the created object.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - apiVersion
                            - kind
                            - name
                            type: object
                          clusterRef:
                            description: |-
                              ClusterRef contains the template required to create a kubean Cluster resource.
                              Deprecated: prefer ClusterTemplate for auto-generated kubean resources.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations propagated to the created
                                  object.
                                type: object
                              apiVersion:
                                description: APIVersion of the target resource, e.g.
                                  cluster.kubean.io/v1alpha1.
                                type: string
                              kind:
                                description: Kind of the target resource, e.g. Cluster
                                  or ClusterOperation.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels propagated to the created object.
                                type: object
                              name:
                                description: Name of the resource to be created.
                                type: string
                              namespace:
                                description: Namespace of the resource. Defaults to
                                  the namespace of the AnsibleConfig.
                                type: string
                              spec:
                                description: Spec encodes the spec body that should
                                  be copied to the created object.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - apiVersion
                            - kind
                            - name
                            type: object
                          clusterTemplate:
                            description: ClusterTemplate defines the parameters required
                              to compose a kubean Cluster resource automatically.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations propagated to the created
                                  object.
                                type: object
                              apiVersion:
                                description: APIVersion of the target resource, e.g.
                                  cluster.kubean.io/v1alpha1.
                                type: string
                              kind:
                                description: Kind of the target resource, e.g. Cluster.
                                type: string
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels propagated to the created object.
                                type: object
                              namespace:
                                description: Namespace of the resource. Defaults to
                                  the namespace of the AnsibleConfig.
                                type: string
                              spec:
                                description: Spec encodes the spec body that should
                                  be copied to the created object.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - apiVersion
                            - kind
                            type: object
                          files:
                            description: Files specifies extra files to be rendered
                              on the target host during cloud-init generation.
                            items:
                              description: File describes an additional file rendered
                                by the bootstrap script.
                              properties:
                                content:
                                  description: Content is the inline file contents.
                                  type: string
                                owner:
                                  description: Owner defines the file owner in the
                                    form user:group. Defaults to root:root.
                                  type: string
                                path:
                                  description: Path is the full path where the file
                                    should be written.
                                  type: string
                                permissions:
                                  description: Permissions is an octal string such
                                    as 0600.
                                  type: string
                              required:
                              - content
                              - path
                              type: object
                            type: array
                          role:
                            description: Role captures the desired Ansible groups
                              for the current node.
                            items:
                              type: string
                            type: array
                        type: object
                      etcdReplicas:
                        description: |-
                          EtcdReplicas is the desired number of etcd Machines. Defaults to 0 which implies a fused control plane/etcd role.
                          Setting this field to a positive value indicates the cluster expects dedicated etcd nodes managed alongside the control plane lifecycle.
                        format: int32
                        type: integer
                      machineTemplate:
                        description: MachineTemplate describes the infrastructure
                          used for control plane Machines.
                        properties:
                          metadata:
                            description: |-
                              Standard object's metadata.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  Annotations is an unstructured key value map stored with a resource that may be
                                  set by external tools to store and retrieve arbitrary metadata. They are not
                                  queryable and should be preserved when modifying objects.
                                  More info: http://kubernetes.io/docs/user-guide/annotations
                                type: object
                              labels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  Map of string keys and values that can be used to organize and categorize
                                  (scope and select) objects. May match selectors of replication controllers
                                  and services.
                                  More info: http://kubernetes.io/docs/user-guide/labels
                                type: object
                            type: object
                          spec:
                            description: |-
                              Specification of the desired behavior of the machine.
                              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
                            properties:
                              bootstrap:
                                description: |-
                                  Bootstrap is a reference to a local struct which encapsulates
                                  fields to configure the Machine’s bootstrapping mechanism.
                                properties:
                                  configRef:
                                    description: |-
                                      ConfigRef is a reference to a bootstrap provider-specific resource
                                      that holds configuration details. The reference is optional to
                                      allow users/operators to specify Bootstrap.DataSecretName without
                                      the need of a controller.
                                    properties:
                                      apiVersion:
                                        description: API version of the referent.
                                        type: string
                                      fieldPath:
                                        description: |-
                                          If referring to a piece of an object instead of an entire object, this string
                                          should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                          For example, if the object reference is to a container within a pod, this would take on a value like:
                                          "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                          the event) or if no container name is specified "spec.containers[2]" (container with
                                          index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                          referencing a part of an object.
                                          TODO: this design is not final and this field is subject to change in the future.
                                        type: string
                                      kind:
                                        description: |-
                                          Kind of the referent.
                                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                        type: string
                                      name:
                                        description: |-
                                          Name of the referent.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                      namespace:
                                        description: |-
                                          Namespace of the referent.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                                        type: string
                                      resourceVersion:
                                        description: |-
                                          Specific resourceVersion to which this reference is made, if any.
                                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                                        type: string
                                      uid:
                                        description: |-
                                          UID of the referent.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                                        type: string
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  dataSecretName:
                                    description: |-
                                      DataSecretName is the name of the secret that stores the bootstrap data script.
                                      If nil, the Machine should remain in the Pending state.
                                    type: string
                                type: object
                              clusterName:
                                description: ClusterName is the name of the Cluster
                                  this object belongs to.
                                minLength: 1
                                type: string
                              failureDomain:
                                description: |-
                                  FailureDomain is the failure domain the machine will be created in.
                                  Must match a key in the FailureDomains map stored on the cluster object.
                                type: string
                              infrastructureRef:
                                description: |-
                                  InfrastructureRef is a required reference to a custom resource
                                  offered by an infrastructure provider.
                                properties:
                                  apiVersion:
                                    description: API version of the referent.
                                    type: string
                                  fieldPath:
                                    description: |-
                                      If referring to a piece of an object instead of an entire object, this string
                                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                      For example, if the object reference is to a container within a pod, this would take on a value like:
                                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                      the event) or if no container name is specified "spec.containers[2]" (container with
                                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                      referencing a part of an object.
                                      TODO: this design is not final and this field is subject to change in the future.
                                    type: string
                                  kind:
                                    description: |-
                                      Kind of the referent.
                                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                    type: string
                                  name:
                                    description: |-
                                      Name of the referent.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  namespace:
                                    description: |-
                                      Namespace of the referent.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                                    type: string
                                  resourceVersion:
                                    description: |-
                                      Specific resourceVersion to which this reference is made, if any.
                                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                                    type: string
                                  uid:
                                    description: |-
                                      UID of the referent.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              nodeDeletionTimeout:
                                description: |-
                                  NodeDeletionTimeout defines how long the controller will attempt to delete the Node that the Machine
                                  hosts after the Machine is marked for deletion. A duration of 0 will retry deletion indefinitely.
                                  Defaults to 10 seconds.
                                type: string
                              nodeDrainTimeout:
                                description: |-
                                  NodeDrainTimeout is the total amount of time that the controller will spend on draining a node.
                                  The default value is 0, meaning that the node can be drained without any time limitations.
                                  NOTE: NodeDrainTimeout is different from `kubectl drain --timeout`
                                type: string
                              nodeVolumeDetachTimeout:
                                description: |-
                                  NodeVolumeDetachTimeout is the total amount of time that the controller will spend on waiting for all volumes
                                  to be detached. The default value is 0, meaning that the volumes can be detached without any time limitations.
                                type: string
                              providerID:
                                description: |-
                                  ProviderID is the identification ID of the machine provided by the provider.
                                  This field must match the provider ID as seen on the node object corresponding to this machine.
                                  This field is required by higher level consumers of cluster-api. Example use case is cluster autoscaler
                                  with cluster-api as provider. Clean-up logic in the autoscaler compares machines to nodes to find out
                                  machines at provider which could not get registered as Kubernetes nodes. With cluster-api as a
                                  generic out-of-tree provider for autoscaler, this field is required by autoscaler to be
                                  able to have a provider view of the list of machines. Another list of nodes is queried from the k8s apiserver
                                  and then a comparison is done to find out unregistered machines and are marked for delete.
                                  This field will be set by the actuators and consumed by higher level entities like autoscaler that will
                                  be interfacing with cluster-api as generic provider.
                                type: string
                              version:
                                description: |-
                                  Version defines the desired Kubernetes version.
                                  This field is meant to be optionally used by bootstrap providers.
                                type: string
                            required:
                            - bootstrap
                            - clusterName
                            - infrastructureRef
                            type: object
                        type: object
                      replicas:
                        description: |-
                          Replicas is the desired number of control plane Machines.
                          Defaults to 1 if not specified.
                        format: int32
                        type: integer
                      version:
                        description: Version is the Kubernetes version for the control
                          plane nodes.
                        minLength: 2
                        type: string
                    required:
                    - ansibleConfigSpec
                    - machineTemplate
                    - version
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
