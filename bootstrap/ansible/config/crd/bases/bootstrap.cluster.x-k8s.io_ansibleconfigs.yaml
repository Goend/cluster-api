---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ansibleconfigs.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: AnsibleConfig
    listKind: AnsibleConfigList
    plural: ansibleconfigs
    singular: ansibleconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster
      jsonPath: .metadata.labels['cluster\.x-k8s\.io/cluster-name']
      name: Cluster
      type: string
    - description: Time duration since creation of AnsibleConfig
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AnsibleConfig defines the Schema for the Ansible bootstrap configuration API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnsibleConfigSpec defines the desired state of AnsibleConfig.
            properties:
              certRef:
                description: CertRef points to the secret containing tls.crt and tls.key
                  which must be baked into the node before ansible runs.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              clusterOperationTemplate:
                description: ClusterOperationTemplate defines the parameters required
                  to compose a kubean ClusterOperation resource automatically.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations propagated to the created object.
                    type: object
                  apiVersion:
                    description: APIVersion of the target resource, e.g. clusterops.kubean.io/v1alpha1.
                    type: string
                  kind:
                    description: Kind of the target resource, e.g. ClusterOperation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels propagated to the created object.
                    type: object
                  namespace:
                    description: Namespace of the resource. Defaults to the namespace
                      of the AnsibleConfig.
                    type: string
                  spec:
                    description: Spec encodes the spec body that should be copied
                      to the created object.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - apiVersion
                - kind
                type: object
              clusterOpsRef:
                description: |-
                  ClusterOpsRef contains the template required to create a kubean ClusterOperation resource.
                  Deprecated: prefer ClusterOperationTemplate for auto-generated kubean resources.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations propagated to the created object.
                    type: object
                  apiVersion:
                    description: APIVersion of the target resource, e.g. cluster.kubean.io/v1alpha1.
                    type: string
                  kind:
                    description: Kind of the target resource, e.g. Cluster or ClusterOperation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels propagated to the created object.
                    type: object
                  name:
                    description: Name of the resource to be created.
                    type: string
                  namespace:
                    description: Namespace of the resource. Defaults to the namespace
                      of the AnsibleConfig.
                    type: string
                  spec:
                    description: Spec encodes the spec body that should be copied
                      to the created object.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - apiVersion
                - kind
                - name
                type: object
              clusterRef:
                description: |-
                  ClusterRef contains the template required to create a kubean Cluster resource.
                  Deprecated: prefer ClusterTemplate for auto-generated kubean resources.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations propagated to the created object.
                    type: object
                  apiVersion:
                    description: APIVersion of the target resource, e.g. cluster.kubean.io/v1alpha1.
                    type: string
                  kind:
                    description: Kind of the target resource, e.g. Cluster or ClusterOperation.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels propagated to the created object.
                    type: object
                  name:
                    description: Name of the resource to be created.
                    type: string
                  namespace:
                    description: Namespace of the resource. Defaults to the namespace
                      of the AnsibleConfig.
                    type: string
                  spec:
                    description: Spec encodes the spec body that should be copied
                      to the created object.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - apiVersion
                - kind
                - name
                type: object
              clusterTemplate:
                description: ClusterTemplate defines the parameters required to compose
                  a kubean Cluster resource automatically.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations propagated to the created object.
                    type: object
                  apiVersion:
                    description: APIVersion of the target resource, e.g. cluster.kubean.io/v1alpha1.
                    type: string
                  kind:
                    description: Kind of the target resource, e.g. Cluster.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels propagated to the created object.
                    type: object
                  namespace:
                    description: Namespace of the resource. Defaults to the namespace
                      of the AnsibleConfig.
                    type: string
                  spec:
                    description: Spec encodes the spec body that should be copied
                      to the created object.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - apiVersion
                - kind
                type: object
              files:
                description: Files specifies extra files to be rendered on the target
                  host during cloud-init generation.
                items:
                  description: File describes an additional file rendered by the bootstrap
                    script.
                  properties:
                    content:
                      description: Content is the inline file contents.
                      type: string
                    owner:
                      description: Owner defines the file owner in the form user:group.
                        Defaults to root:root.
                      type: string
                    path:
                      description: Path is the full path where the file should be
                        written.
                      type: string
                    permissions:
                      description: Permissions is an octal string such as 0600.
                      type: string
                  required:
                  - content
                  - path
                  type: object
                type: array
              role:
                description: Role captures the desired Ansible groups for the current
                  node.
                items:
                  type: string
                type: array
              varsConfigRefs:
                description: VarsConfigReferences enumerates the Config resources
                  consumed during vars rendering.
                properties:
                  business:
                    description: Business references the user-managed Config containing
                      business-level vars.
                    minLength: 1
                    type: string
                  fixed:
                    description: Fixed references the user-managed Config containing
                      fixed/system vars.
                    minLength: 1
                    type: string
                  infra:
                    description: Infra references the (usually auto-generated)
                      Config containing infrastructure facts.
                    minLength: 1
                    type: string
                required:
                - business
                - fixed
                type: object
            type: object
          status:
            description: AnsibleConfigStatus defines the observed state of AnsibleConfig.
            properties:
              conditions:
                description: Conditions defines current service state of the AnsibleConfig.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              dataSecretName:
                description: DataSecretName is the name of the secret that stores
                  the bootstrap data script.
                type: string
              failureMessage:
                description: FailureMessage will be set on non-retryable errors
                type: string
              failureReason:
                description: FailureReason will be set on non-retryable errors
                type: string
              initialization:
                description: |-
                  Initialization mirrors the v1beta2 bootstrap contract status block.
                  When DataSecretCreated=true bootstrap data is guaranteed to exist.
                properties:
                  dataSecretCreated:
                    description: DataSecretCreated becomes true once the bootstrap
                      data Secret exists.
                    type: boolean
                type: object
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
              postBootstrapCompleted:
                description: PostBootstrapCompleted indicates whether the controller
                  finished creating kubean resources.
                type: boolean
              ready:
                description: Ready indicates the BootstrapData field is ready to be
                  consumed
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
